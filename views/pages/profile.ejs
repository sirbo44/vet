<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head'); %>
<style>
  .card{
    margin-left:30%;
  }
  .card-body{
    margin-left: 50%;
  }
  #title{
    margin-left: 42%;
  }
</style>
<body>
  <%- include('../partials/navbar'); %>
    <h1 id="title">Your exams</h1>
    <%if (pets.length>=1){%>
      <%for (let i=0;i<pets.length;i++) {%>
        <div class="card mb-3 " style="max-width: 540px;">
          <div class="row g-0">
            <div class="col-md-8">
              <div class="card-body">
                <h5 class="card-title">Pet id: <%= pets[i].id %></h5>
                <p class="card-text">Exams : <%= pets[i].exams %></p>
                <p class="card-text"><small class="text-body-secondary">Owned by: <%= pets[i].owner %></small></p>
              </div>
            </div>
          </div>
        </div>
      <% } %>
    <%} else {%>
      <p>There are not pets for this user</p>
    <%}%>
    <%- include('../partials/footer'); %>
    <%- include('../partials/bottom'); %>
</body>
</html>